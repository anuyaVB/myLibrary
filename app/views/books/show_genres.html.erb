<h1><%= @genre.name %></h1>

<ul>
   <% @genre.books.each do |c| %>
   <li><%= link_to c.bookname, {:action => 'show', :id => c.id} %></li>
   <% end %>
</ul>
<%= link_to 'Back', {:action => 'list'} %>